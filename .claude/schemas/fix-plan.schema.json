{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Fix Plan",
  "description": "Schema for Fix Planner dependency-ordered fix plan",
  "type": "object",
  "required": ["hasLayer0", "hasWorkPackages", "hasVerificationCheckpoints"],
  "properties": {
    "hasLayer0": { "const": true, "description": "Must contain ## Layer 0 (auto-fixes)" },
    "hasWorkPackages": { "const": true, "description": "Must contain at least one work package (WP or ### Layer)" },
    "hasVerificationCheckpoints": { "const": true, "description": "Must contain verification steps between layers" }
  }
}
